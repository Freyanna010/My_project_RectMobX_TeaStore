export type Supplement = {
  id: string;
  name: string;
  img: string;
  isAdd: boolean;
};

export type CollectionSupplements = {
  id: string;
  name: string;
  isEnough: boolean;
};

export type Tea = {
  id: string;
  name: string;
  img: string;
  isEnough: boolean;
  description: string;
};